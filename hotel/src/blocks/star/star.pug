mixin star(options)  
  label.star
    input.star__btn(
      type="radio" 
      name=options.name 
      value=options.value
      checked=options.checked
      disabled=options.disabled
    )
    span.star__style(class=(options.theme) ? "star__style_" + options.theme : "") 
