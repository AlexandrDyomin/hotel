mixin author(options)
  .author
   img.author__photo(src=options.url, alt="")
   .author__info 
    .author__name=options.name
    time.author__additional-info(pubdate datetime="")=options.additionalInfo